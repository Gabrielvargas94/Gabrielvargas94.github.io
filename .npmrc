# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false
# Hoist rollup/esbuild optional native binaries so Astro + Vite resolve them.
# Without this, rollup's `@rollup/rollup-*` platform packages aren't visible
# in the isolated node_modules layout pnpm uses by default.
shamefully-hoist=true
